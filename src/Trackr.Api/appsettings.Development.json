{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // For the `dotnet watch` loop, where only the db container is running and its
    // port is published to the host. When the backend itself runs in Compose, the
    // ConnectionStrings__Trackr environment variable overrides this and points at
    // the `db` service instead. Dev credentials only - never used in production.
    "Trackr": "Host=localhost;Port=5433;Database=trackr;Username=trackr;Password=trackr_dev"
  }
}
